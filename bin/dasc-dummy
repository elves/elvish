#!/bin/sh
tube=$1; shift

{
    while read line; do
        printf 'response: %s\n' "$line"
    done
} <&$tube &

child=$!

while read line; do
    printf '%s\n' "$line" >&$tube
done

printf '{"Exit":{}}\n' >&$tube
kill $child
