package main

// TODO: Move to etk?

/*
func Compose(elems ...any) (etk.View, etk.React) {
}

func crud2() {
	list := c.Subcomp(...)
	Compose(
		prefix,
		list.FixedWidth(20), Group(name, surname).FillWidth(),
		create.ContentWidth(), update.ContentWidth(), delete.FillWidth())
}
*/
